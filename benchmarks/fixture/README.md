# Benchmark fixture

This intentionally small Python project gives every benchmark run a fresh, deterministic workspace.
It contains independent modules as well as coupled service behavior so the routing policy has both
parallelizable and non-parallelizable cases.
